# 数据审计与数据画像

## 附件映射

当前项目只有 `B题：舰船烟幕遮蔽干扰优化.docx`。文件大小 15,135 字节，SHA-256 为 `e891f635bcb4182517c166d12f1e4f3d05c77e9c18b27a14302c7561f7f2a638`；包含 22 个段落、0 个表格、0 张图片、0 个嵌入附件。该文件是 Q1–Q4 共用的题目正文与常数来源。

项目中不存在 Excel、CSV、MAT、图片或其他场景数据，因此没有 Sheet、字段行、样本量、时间范围、类别不平衡或异常值可统计。原始 DOCX 未被覆盖或修改，`workspace/data_clean/` 为空，也没有进行删除、插补、缩放或编码转换。

## 已给数据

可直接使用的常数包括：舰速 `7.71 m/s`、舰船等效半径 `80 m`、无人机速度 `28 m/s`、作战半径 `12000 m`、单机载弹 `3`、响应延时 `2 s`、最小投弹间隔 `1 s`、起爆延时 `3.5 s`、烟幕半径 `120 m`、恒定期 `18 s`、衰减期 `5 s`、导弹速度 `320 m/s`、探测距离 `8000 m`、视场半角 `15°`。

## 缺失和语义冲突

- 没有舰船、导弹、无人机的初始坐标与航向。
- 没有导弹追踪律、视场参考方向和 Q4 导弹批次表。
- 没有干扰弹延时阶段的水平运动、烟幕漂移速度、能耗函数和安全距离。
- Q4 提到“不同速度”，但全局只给出 `320 m/s`，需确认它是统一速度还是默认值。
- “稳定扩张”与“起爆后瞬时达到最大半径”表述不一致；当前按数值更明确的瞬时达到 120 m 记录，但不把这一处理标为已获人类确认。

## 就绪结论

题面常数足以支持结构推导、相对坐标优化和条件性方法筛选；绝对坐标与真实场景调度仍受初始状态缺失限制。Q1 已利用不依赖绝对初态的结构证书完成；Q2 可继续做相对几何与时序优化，但首弹绝对可达必须等缺失场景输入补齐。Q3/Q4 仍有各自额外缺项。详细逐问状态见 `data_profile.json`。

## Q2 状态更新（继承 Q1 人工口径）

建模手已在 Q1 确认 M1 纯追踪与 S1 固定云心名义模型，因此 Q2 不再被烟幕运动解释阻塞。现有常数足以进行相对坐标下的多烟幕圆盘并集、弹数容量、时序衔接和扰动探针。仍缺无人机初始/基地位置、作战半径基准点和绝对任务时钟，所以 Q2 只能输出参数化空间关系和相邻投放可达条件，不能编造绝对首弹坐标。
